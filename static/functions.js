function register(){
    //Check both passwords are the same if not throw error message
    //Start an XMLHttpRequest and pass in the form data as a json
    //object and send to /connections/add
}

function login(){
    //Turn form into json data and send to /connections/login
}
